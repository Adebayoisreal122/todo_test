import { Routes } from '@angular/router';
import { TodoappComponent } from './todoapp/todoapp.component';

export const routes: Routes = [

    { path: '', component: TodoappComponent, title: 'todo app' },

];
